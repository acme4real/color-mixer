# Color Mixer
## Purpose
Allows users to mix two colors and see the resulting color.

## Setup Steps
1. Clone the repository.
2. Install Python and Flask.
3. Run `python app.py` to start the server.
4. Open your browser and go to `http://localhost:5000`.